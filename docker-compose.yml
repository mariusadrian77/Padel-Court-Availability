services:
  padel-monitor:
    build: .
    container_name: padel-monitor
    restart: unless-stopped
    environment:
      - TZ=Europe/Amsterdam
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - padel-data:/app/data

volumes:
  padel-data:
